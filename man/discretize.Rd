% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discretize.R
\name{discretize}
\alias{discretize}
\title{Discretization}
\usage{
discretize(
  x,
  n = 5,
  method = "natural",
  large = 3000,
  prop = 0.15,
  seed = 123456789,
  thr = 0.4,
  iter = 100,
  bps = NULL,
  right_closed = TRUE
)
}
\arguments{
\item{x}{A vector.}

\item{n}{(optional) Number of classes.}

\item{method}{(optional) Discretization method. One of
\code{"sd"}, \code{"equal"}, \code{"geometric"}, \code{"quantile"},
\code{"manual"}, \verb{"natural("jenks")"}, or \code{"headtail"("headtails")}.}

\item{large}{(optional) Threshold sample size for natural breaks sampling.}

\item{prop}{(optional) Sampling proportion used when \code{method = "natural"}
and the input size exceeds \code{large}.}

\item{seed}{(optional) Random seed used for sampling in natural breaks.}

\item{thr}{(optional) Threshold used in the head/tail breaks algorithm.}

\item{iter}{(optional) Maximum number of iterations for head/tail breaks.}

\item{bps}{(optional) Numeric vector of manual breakpoints used when
\code{method = "manual"}.}

\item{right_closed}{(optional) Logical. If \code{TRUE}, intervals are right-closed
(e.g., \verb{(a, b]}). If \code{FALSE}, intervals are left-closed \verb{[a, b)}.}
}
\value{
A discretized integer vector.
}
\description{
Discretize a numeric vector into categorical classes using several
commonly used discretization methods. Missing values (\code{NA}/\code{NaN})
are ignored and returned as class \code{0}.
}
\note{
If \code{x} is not numeric, it will be converted to
integer categories via \code{as.factor()}.
}
\examples{
set.seed(42)
infoxtr::discretize(stats::rnorm(99,1,10), n = 5, method = 'natural')

}
